{
    "name": "chiron/app",
    "type": "project",
    "description": "Chiron app",
    "homepage": "https://github.com/ncou/app",
    "keywords": [
        "chiron",
        "app"
    ],
    "license": "MIT",
    "require": {
        "php": "^7.3",
        "chiron/chiron": "^2.5"
    },
    "require-dev": {
        "phpunit/phpunit": "^8.0",
        "phpstan/phpstan": "^0.12.0"
    },
    "autoload": {
        "psr-4": {
            "App\\": "src/"
        }
    },
    "autoload-dev": {
        "psr-4": {
            "Tests\\": "tests/"
        }
    },
    "scripts": {
        "post-create-project-cmd": [
            "@php -r \"file_exists('app/.env') || copy('app/.env.example', 'app/.env');\"",
            "bash chiron encrypt::key -m app/.env"
        ],
        "start": "php -S localhost:8080 -t public/",
        "serve": "bash chiron serve",
        "test": "phpunit --colors=always"
    },
    "config": {
        "process-timeout": 0,
        "sort-packages": true
    }
}
